{
    "physical.name": "LiH",
    "model.embed.use": true,
    "model.slatermodel.n_determinants": 20,
    "optimization.learning_rate": 1.5e-2,
    "optimization.n_epochs": 500,
    "integration.train.n_burnin_steps": 10,
    "integration.valid.n_burnin_steps": 10,
    "integration.eval.n_burnin_steps": 10,
    "evaluation.n_epochs_min": 10,
    "evaluation.n_epochs_max": 20,
    "parallel_trainer.use": true,
    "parallel_trainer.scheduling_max_age": 20,
    "parallel_trainer.lr_factor": 10,
    "parallel_trainer.shared_weights": "['embedding', 'symmetric', 'backflow_factor_general', 'backflow_shift']",
    "parallel_trainer.config_changes": "[{'physical.ion_positions': [[0, 0, 0], [x, 0, 0]]} for x in [2.5, 2.6, 2.7, 3.2, 3.3, 3.4, 3.5, 3.6]]"
}