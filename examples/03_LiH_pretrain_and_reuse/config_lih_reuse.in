{
    "physical.name": "LiH",
    "physical.ion_positions": "[[0, 0, 0], [3.0, 0, 0]]",
    "model.embed.use": true,
    "model.slatermodel.n_determinants": 20,
    "optimization.learning_rate": 1.5e-3,
    "optimization.n_epochs": 500,
    "integration.train.n_burnin_steps": 10,
    "integration.valid.n_burnin_steps": 10,
    "integration.eval.n_burnin_steps": 10,
    "evaluation.n_epochs_min": 200,
    "evaluation.n_epochs_max": 500,
    "model.reuse_weights": "ReuseWeightsConfig(reuse_dirs=['../example_run_pretrain/wf_0'], weights=['embedding', 'backflow_factor_general', 'backflow_shift', 'symmetric'])"
}
